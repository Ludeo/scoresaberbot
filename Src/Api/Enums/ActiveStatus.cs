namespace Bot.Api.Enums
{
    /// <summary>
    ///     Enum that displays if a player is active or inactive.
    /// </summary>
    public enum ActiveStatus
    {
        /// <summary>
        ///     Shows that the player is active.
        /// </summary>
        Active,

        /// <summary>
        ///     Shows that the player is inactive.
        /// </summary>
        Inactive,
    }
}